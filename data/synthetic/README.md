# Datos sintéticos

Los CSV de esta carpeta se generan con:

```bash
python -m src.generate_synthetic
```

Son **100% ficticios** (seed=42 por defecto). No versionamos los CSV grandes por defecto;
regenerarlos es parte del flujo del README.
